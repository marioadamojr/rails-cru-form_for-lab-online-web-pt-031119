<h1>Song Show Page</h1>
<h1><%= @song.name %></h1>

<div>
  <ul>
  <% @song.each do |song| %>
    <li><%= link_to song.artist_id, song_path(song) %></li>
    <li><%= link_to song.genre_id, song_path(song) %></li>
  <% end %>
  </ul>
</div>
